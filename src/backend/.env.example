# PostgreSQL connection string
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/myfitideas?schema=public"

# Replace with a long, random value for local development.
# Never commit the real secret used by any shared or production environment.
JWT_SECRET="replace-with-a-long-random-development-secret"

PORT=3000
NODE_ENV=development
